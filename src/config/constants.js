export const TABLES = {
  chargers: 'chargers',
  loadPatterns: 'load_patterns',
}

export const CSV_FIELDS = {
  chargersFile: 'chargers',
  loadPatternFile: 'loadPattern',
  activePrefix: 'active_',
}

export const SIMULATION_SIGNAL = {
  type: 'PWM',
  target: 'web-simulation',
}

export const DEFAULT_LIMITS = {
  uploadFileSizeBytes: 1024 * 1024,
}
